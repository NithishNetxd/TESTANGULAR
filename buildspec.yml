version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g @angular/cli
  pre_build:
    commands:
      - npm install -f
  build:
    commands:
      - ng build
artifacts:
  files:
    - dist/test/server/**
